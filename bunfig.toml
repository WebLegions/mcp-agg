[test]
# Preload mock utility and DOM APIs for tests
# Note: happy-dom is configured to preserve Bun's native fetch/EventTarget
preload = ["./script/mock-prehook.ts", "./script/happy-dom.ts"]

# Coverage configuration
coverageSkipTestFiles = true
