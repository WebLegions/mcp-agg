{
  "name": "mcp-agg",
  "version": "0.3.0",
  "description": "MCP Aggregator - Aggregate multiple Model Context Protocol servers into a single endpoint with unified tool discovery and execution",
  "type": "module",
  "scripts": {
    "start": "bun src/app.ts",
    "bench:json": "bun --expose-gc script/bench_json_parse.ts",
    "bench:logger": "bun --expose-gc script/bench_logger.js",
    "bench:validator": "bun --expose-gc script/bench_validator.js",
    "build": "docker build --build-arg \"BUILD_TS=$(bun -e 'console.log(new Date().toISOString());')\" --target prod -t mcp-agg:latest . && docker build --build-arg \"BUILD_TS=$(bun -e 'console.log(new Date().toISOString());')\" --target scan . && docker build --build-arg \"BUILD_TS=$(bun -e 'console.log(new Date().toISOString());')\" --target logs --output type=local,dest=./build-logs .",
    "build:grype": "docker build --build-arg \"BUILD_TS=$(bun -e 'console.log(new Date().toISOString());')\" --target scan .",
    "clean": "bun script/rimraf.ts -- -q var dist build build-logs coverage node_modules .cache NUL *.log *.cpuprofile",
    "cluster": "bun src/cluster.ts",
    "dev": "bun --hot src/app.ts",
    "kill": "bun script/kill.ts",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "mcp": "bun src/cli/index.ts mcp",
    "publish:npm": "npm login && npm publish --access public",
    "release": "bun script/release.ts",
    "rimraf": "bun script/rimraf.ts",
    "test": "bun run lint:fix && bun script/run-tests.ts --port 4000 && bun script/coverage-report.ts",
    "test:verbose": "bun run lint && tsc && bun script/run-tests.ts --port 4000 --verbose && bun script/coverage-report.ts",
    "test:ci": "bun test ci",
    "test:watch": "bun test --watch"
  },
  "dependencies": {
    "@fastify/cors": "^11.0.0",
    "@fastify/helmet": "^12.0.0",
    "@fastify/rate-limit": "^10.3.0",
    "@fastify/static": "^8.3.0",
    "@fastify/swagger": "^9.6.0",
    "@types/node": "^24.9.2",
    "fastify": "^5.2.0",
    "juris": "^0.9.0",
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.2",
    "@happy-dom/global-registrator": "^20.0.10",
    "@types/bun": "^1.1.6"
  },
  "engines": {
    "bun": ">=1.3.0"
  },
  "bin": {
    "mcp-agg": "script/bin.js"
  },
  "files": [
    "package.json",
    "README.md",
    "LICENSE"
  ],
  "keywords": [
    "mcp",
    "model-context-protocol",
    "aggregator",
    "tools",
    "ai",
    "llm",
    "fastify",
    "bun",
    "typescript",
    "rest-api",
    "cli"
  ],
  "author": "eram@weblegions.com",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/eram/mcp-agg.git"
  },
  "bugs": {
    "url": "https://github.com/eram/mcp-agg/issues"
  },
  "homepage": "https://github.com/eram/mcp-agg#readme",
  "private": false
}